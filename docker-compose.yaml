version: '3'

services:
  frontend:
    build: ./frontend
    ports:
      - "443:443"
    links:
      - backend
    volumes:
      - ./docs:/app/build


  backend:
    build: ./backend
    ports:
      - "8000:8000"
    networks:
      default:
        aliases:
          - backend
